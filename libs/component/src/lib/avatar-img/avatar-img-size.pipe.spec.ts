import { AvatarImgSizePipe } from './avatar-img-size-class.pipe';

describe('AvatarImgSizePipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarImgSizePipe();
    expect(pipe).toBeTruthy();
  });
});
