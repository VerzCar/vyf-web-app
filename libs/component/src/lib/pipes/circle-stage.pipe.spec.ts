import { CircleStagePipe } from './circle-stage.pipe';

describe('CircleStagePipe', () => {
  it('create an instance', () => {
    const pipe = new CircleStagePipe();
    expect(pipe).toBeTruthy();
  });
});
